
{
  "name": "zeeeco",
  "version": "1.0.0",
  "description": "Zeee.co",
  "author": "Jan",
  "private": true,
  "scripts": {
    "server":  "bundle exec middleman server --port 8888",
    "start":   "npm run server",

    "build":   "bundle exec middleman build ",
    "github":  "npm run build; bundle exec middleman deploy ",
    "stage":   "npm run build ; cd build ; surge . -d zeeco.surge.sh",
    "prod":    "npm run build ; cd build ; surge . -d zeee.co"
  }
}
